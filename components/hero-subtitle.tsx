export const HeroSubtitle = ({ children }: { children: React.ReactNode }) => {
    return (
        <p className="text-primary text-[22px] mb-[48px] text-center">
            {children}
        </p>
    )
}
